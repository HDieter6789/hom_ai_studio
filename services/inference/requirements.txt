httpx==0.27.2
pydantic==2.9.2
-e ../../packages/hom_core

pytest==8.3.3

# Real serving (only needed to actually run vLLM; deploy_model() fails
# with a clear VLLMNotInstalled error otherwise):
# vllm
